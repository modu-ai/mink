{
  "name": "mink-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E speedrun tests for MINK install wizard",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:list": "playwright test --list"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
